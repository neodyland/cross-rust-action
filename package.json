{
    "name": "cross-rust",
    "private": true,
    "devDependencies": {
        "@biomejs/biome": "2",
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.8.3"
    },
    "type": "module",
    "scripts": {
        "format": "biome format --write .",
        "build": "bun build src/setup.ts --production --target bun --outfile setup.js --minify"
    }
}
