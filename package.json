{
    "name": "cross-rust",
    "private": true,
    "devDependencies": {
        "@biomejs/biome": "^1.9.4",
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "type": "module",
    "scripts": {
        "format": "biome format --write .",
        "build": "bun build src/setup.ts --production --target bun --outfile setup.js --minify"
    }
}
